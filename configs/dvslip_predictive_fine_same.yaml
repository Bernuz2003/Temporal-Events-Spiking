extends: dvslip_predictive_fine_future.yaml

experiment:
  name: dvslip_predictive_fine_same

continuation:
  objective:
    mode: fine_same
    normalization_report: artifacts/predictive_diagnostics/fine_same_probe.json
