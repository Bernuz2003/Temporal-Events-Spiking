# Installazione minimale. Per lo sviluppo e DVS-GC usare `make install-dev`.
# Il metadata PyPI di dvsgc==0.1.2 vincola una vecchia versione di SpikingJelly;
# dvsgc viene quindi installato separatamente con --no-deps dal Makefile e dal container.
torch>=2.1
torchvision>=0.16
numpy>=1.24,<2.0
PyYAML>=6.0
matplotlib>=3.7
tqdm>=4.66
spikingjelly==0.0.0.0.14
